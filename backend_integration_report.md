# Backend Integration Report

**Generated on:** $(date)

## 1. Backend Verification
- **Framework**: FastAPI
- **Database**: PostgreSQL (via Railway)
- **ORM**: SQLAlchemy + Alembic
- **Standard Response**: `{ success, message, data }`

## 2. Endpoint Audit
| Method | Path | Status | Response Format |
| :--- | :--- | :--- | :--- |
| POST | `/irisk/auth/send-otp` | ✅ Verified | Standard |
| POST | `/irisk/auth/verify-otp` | ✅ Verified | Standard |
| POST | `/irisk/fire/uiic/vusp/calculate` | ✅ Verified | Standard |
| POST | `/irisk/fire/uiic/calculate/pdf` | ✅ Verified | Standard (Fixed) |
| POST | `/api/premium/uvgs/calculate` | ✅ Verified | Standard (New) |
| GET | `/api/rates/ubgr` | ✅ Verified | Standard (New) |
| GET | `/api/rates/uvgs` | ✅ Verified | Standard (New) |

## 3. Deployment Status
- **Router Prefixes**: `main.py` explicitly handles `/api/premium` and `/api/rates`.
- **Migrations**: Autogenerated and Applied (`alembic upgrade head`).
- **Global Error Handling**: Middleware catches 500s and returns JSON.

## 4. Frontend Compatibility
The backend is now fully compatible with the requested Flutter `ApiService`. All logic (Timeouts, JSON wrapping, Error Codes) matches the standard protocol.

**Status: READY FOR PRODUCTION**
